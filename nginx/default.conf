upstream django {
    server entres:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://django;
    }

    location /static/ {
        alias /travail/app/static/;
    }

    location /media/ {
        alias /travail/app/media/;
    }
}
